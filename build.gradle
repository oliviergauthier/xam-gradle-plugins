task wrapper(type: Wrapper) {
    gradleVersion = '3.4.1'
}

task publishAll(dependsOn : ['bintrayUpload', 'publishPlugins'], description:'Upload to bintray and publish plugins', group:'Publishing') {}